一、聚合函数介绍
聚合函数也称之为多行函数，组函数或分组函数。聚合函数不像单行函数，聚合函数对行的分组进行操作，对每组给出一个结果。如果在查询中没有指定分组，那么聚合函数则将查询到的结果集视为一组。
聚合函数的类型：
AVG平均值：返回一个表达式的平均值，expression是一个字段。
eg：返回Products表中Price字段的平均值：SELECT AVG(Price) AS AveragePrice FROM Products;
COUNT计数：返回查询的记录总数，expression参数是一个字段或者*号。
eg:返回Products字段总共有多少条记录：SELECT COUNT(ProductID) AS NumberOfProducts FROM Products;
MAX最大值:返回字段expression中的最大值。
eg:返回数据表Products中字段Price的最大值：SELECT MIN(Price) AS MinPrice FROM Products;
MIN最小值:返回字段expression中的最小值。
eg：返回数据表Products中字段Price的最小值：SELECT MIN(Price) AS MinPrice FROM Products;
SUM合计:返回指定字段的总和。
eg:计算OrderDetails表中字段Quantity的总和:SELECT SUM(Quantity) AS TotalltemsOrdered FROM OrderDetails;

聚合函数的语法：
SELECT 列名 FROM 表名 [WHERE 条件 GROUP BY 数据 ORDER BY 数据]；
使用聚合函数的原则：
DISTINCT使得函数只考虑不重复的值；
所有聚合函数忽略空值。为了用一个值代替空值，用IFNULL或COALESCE函数。

二、AVG和SUM函数
1、AVG函数
对分组数据做平均值运算。
arg：参数类型只能是数字类型。
2、SUM函数
对分组数据求和。
arg：参数类型只能是数字类型。
3、MIN函数
求分组中最小数据。
arg：参数类型可以是字符、数字、日期。
4、MAX函数
求分组中最大数据。
arg：参数类型可以是字符、数字、日期。
5、COUNT函数
返回分组中的总行数。
COUNT函数有三种格式：
（1）COUNT(*):返回表中满足SELECT语句的所有列的行数，包括重复行，包括有空值列的行。
（2）COUNT(expr):返回在列中的由expr指定的非空值的数。
（3）COUNT(DISTINCT expr):返回在列中的由expr指定的唯一的非空值的数。
使用DISTINCT关键字：
（1）COUNT(DISTINCT expr)返回对于表达式expr非空并且值不相同的行数。
（2）显示EMPLOYEES表中不同部门数的值。
6、组函数和Null值
在组函数中使用IFNULL函数
