一、定义空值
1、null是一个未分配的、未知的，或不适用的值
2、null不是0，也不是空格
SELECT 列 FROM 表；
如果一行中的某个列缺少数据值，该值被置为null，或者说包含一个空。
空是一个难以获得的、未分配的、未知的，或不适用的值。空和0或者空格不相同。0是一个数字，而空格是一个字符。

二、算术表达式中的空值
包含空值的算术表达式计算结果为空：
SELECT 列名 FROM 表名；//列名可以通过式子计算
