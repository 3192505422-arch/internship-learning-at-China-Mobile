一、多表查询简介
笛卡尔乘积：当一个连接条件无效或被遗漏时，其结果是一个笛卡尔乘积，其中所有行的组合都被显示。第一个表中的所有行连接到第二个表中的所有行。一个笛卡尔乘积会产生大量的行，其结果没有什么用。应该在WHERE子句中始终包含一个有效的连接条件，除非有特殊的需求，需要从所有表中组合所有行。
多表查询分类：
sql92标准：内连接（等值连接、非等值连接、自连接）。
sql99标准：内连接、外连接（左外、右外、全外（MySQL不支持全外连接））、交叉连接。

二、sql92标准中的查询
1、等值连接
为了确定一个雇员的部门名，需要比较两个表中两个列的值。两个表中的列的值必须相等。
特点：
（1）多表等值连接的结果为多表的交集部分；
（2）n表连接、至少需要n-1个连接条件；
（3）多表不分主次，没有顺序要求；
（4）一般为表起别名，提高阅读性和性能；
（5）可以搭配排序、分组、筛选...等子句使用；
2、等值连接的使用
(1)SELECT子句指定要返回的列名
(2)FROM子句指定数据库必须访问的两个表
(3)WHERE子句指定表连接
3、增加搜索条件
使用AND操作符附加搜索条件
表名.列名=表名.列名 AND 列名=...；
4、添加查询条件
除连接之外，可能还要求用WHERE子句在连接中限制一个或多个表中的行。
5、限制不能缺的列
在多表中使用表前缀限制修改列名
用表前缀改善性能
用列别名区别有相同名称但在不同表中的列
6、使用表别名
使用表别名简化查询
使用表别名改善性能
7、表别名定义原则
（1）表别名不易过长，短一些更好
（2）表别名应该是有意义的
（3）表别名只对当前的SELECT语句有效
8、多表连接
为了连接n和表，最少需要n-1个连接条件
9、非等值连接
一个非等值连接是一种不同于等值操作的连接条件。使用不同于等于（=）的操作符获得关系。
10、自连接
连接一个表到它自己。有时需要连接一个表到它自己。

三、sql99标准中的查询
1、交叉连接
CROSS JOIN子句导致两个表的交叉乘积
该连接和两个表之间的笛卡尔乘积是一样的
SELECT 列名 FROM 表名 CROSS JOIN 另一个表；
2、自然连接
NATURAL JOIN子句基于两个表之间有相同名字的所有列。
它从两个表中选择在所有的匹配列中有相等值的行。
如果有相同名字的列的数据类型不同，返回一个错误。
SELECT 列名 FROM 表名 NATURAL JOIN 另一个表；
3、内连接
语法：
SELECT查询列表；
FROM 表1 别名；
INNER JOIN 连接表（INNER关键字可省略）；
ON 连接条件；
4、用ON子句返回记录
在内连接中使用INNER JOIN指定连接的表，用ON指定连接条件。
5、内、外连接查询
（1）在SQL1999中，连接两个表，仅返回匹配的行的连接，称为内连接。
（2）在两个表之间的连接，返回内连接的结果，同时还返回不匹配行的左（或右）表的连接，称为左（或右）外连接。
（3）在两个表之间的连接，返回内连接的结果，同时还返回左和右连接，称为全外连接。
5、孤儿数据
指被连接的列的值为空的数据。
6、左外连接
左边的表中即使没有与另一个表中匹配的行，该查询也会取回表中所有的行。
SELECT 列名 FROM 表名 LEFT OUTER JOIN 表名 ON （列名=列名）；
7、右外连接
右边的表中即使没有与另一个表中匹配的行，该查询也会取回表中所有的行。
SELECT 列名 FROM 表名 RIGHT OUTER JOIN 表名 ON （列名=列名）；
8、全外连接
可以使用union实现全完连接：
UNION：可以将两个查询结果集合并，返回的行都是唯一的，如果对整个结果集合使用了DISTINCT。
UNION ALL：只是简单的将两个结果合并后就返回。这样如果返回的两个结果集中有重复的数据，那么返回的结果集就会包含重复的数据了。
（SELECT 投影列 FROM 表名 LEFT OUTER JOIN 表名 ON 连接条件）
UNION
（SELECT 投影列 FROM 表名 RIGHT OUTER JOIN 表名 ON 连接条件）
