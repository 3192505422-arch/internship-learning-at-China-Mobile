一、元素通用操作
1、元素内部的可见文本
通过 Locator对象的inner_text()方法可以获取元素的内部文本。
如果Locator选择到的元素是多个，可以使用all_inner_texts获取所有匹配的文本，放到列表中返回。
2、元素内部的不可见文本
可以用Locator对象的text_content()或者all_text_contents()方法获取单个或者多个匹配对象文本。
比如：
<p id="source">
  <span id='text'>看一下<br>这个内容<br>如何变化</span>
  <span style="display:none">隐藏内容</span>
</p>
获取p元素内部所有内容：
lc = page.locator("#source")
print('innerText:',   lc.inner_text())
print('--------------')
print('textContent:', lc.text_content())
（1）获取元素属性：可以使用Locator对象的get_attribute方法
（2）获取元素内部Html：可以使用Locator对象的inner_html方法
（3）点击：单击元素click方法，双击元素dblclick方法
（4）悬停：可以使用Locator对象的hover方法
（5）等待元素可见：可以使用Locator对象的wait_for方法
（6）判断元素是否可见：可以使用 Locator对象的 is_visible 方法（不会等待元素出现，而是立即返回True或False）

二、输入框操作
（1）文本框输入：单行文本框  input  或者 多行文本框  textarea  都可以使用 Locator对象的 fill 方法进行输入
（2）文本框清空：要清空 单行文本框  input  或者 多行文本框  textarea 的内容，都可以使用 Locator对象的 clear 方法
（3）获取输入框里面的文字：如果要获取输入框  <input>  ， <textarea>  对应的用户输入文本内容，用 input_value 方法
（4）文件输入框：要设置选中的文件，可以使用 Locator 对象的 set_input_files 方法
（5）radio单选：如果要点选 radio框， 可以使用 Locator对象的 check 方法
如果要取消选择 radio框， 可以使用 Locator对象的 uncheck 方法
如果我们要判断 radio框 是否选中，可以使用 Locator对象的 is_checked 方法
（6）checkbox多选：如果要点选 checkbox框， 可以使用 Locator对象的 check 方法
如果要取消选择 checkbox框， 可以使用 Locator对象的 uncheck 方法
如果我们要判断 checkbox 框 是否选中，可以使用 Locator对象的 is_checked 方法
（7）select元素操作：使用  select  元素对应的 Locator对象的 select_option 方法

三、网页操作
（1）打开网址/刷新/前进/后退：分别调用Page对象的goto/reload/go_back/go_forward 方法
（2）获取整个网页对应的HTML：调用Page对象的content方法
（3）title：调用Page对象的title方法
（4）set-viewport-size：调用 Page 对象的  set_viewport_size 方法
