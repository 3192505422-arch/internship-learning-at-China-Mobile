一、子查询的介绍
1、用子查询解决问题
假如要写一个查询来找出挣钱比Abel的薪水还多的人。为了解决这个问题，需要两个查询：一个找出Abel的收入，第二个查询找出收入高于Abel的人。可以用组合两个查询的方法解决这个问题。内查询或子查询返回一个值给外查询或主查询。使用一个子查询相当于执行两个连续查询并且用第一个查询的结果作为第二个查询的搜索值。
可以将子查询放在许多的SQL子句中，包括：
（1）WHERE子句
（2）HAVING子句
（3）FROM子句

2、使用子查询
SELECT a列名 FROM 表名 WHERE b列名>(SELECT b列名
                                  FROM 表名
                                  WHERE a列名 = 一个值)
原则：
（1）子查询放在圆括号中。
（2）将子查询放在比较条件的右位
（3）在单行子查询中用单行运算符，在多行子查询中用多行运算符。

3、子查询的类型
单行子查询：返回一条数据
多行子查询：返回多行数据

二、单行子查询
